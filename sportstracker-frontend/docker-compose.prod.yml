version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    container_name: frontend-app
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
